name: featherpeakfurygame
description: "Offline hiking trip preparation and load planning utility"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.3+4

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  flutter_animate: ^4.5.2
  fl_chart: ^1.2.0
  gap: ^3.0.1
  flutter_slidable: ^4.0.3
  image_picker: ^1.2.3
  audioplayers: ^6.8.1
  webview_flutter: ^4.14.1
  path_provider: ^2.1.6
  intl: ^0.20.3
  uuid: ^4.6.0
  # Mobile attribution: classifies an install as organic (found on the App
  # Store) or non-organic (arrived through a OneLink campaign). Pinned to an
  # exact version that no sibling project uses.
  appsflyer_sdk: 6.17.9+1
  app_tracking_transparency: ^2.0.7
  # Daily local reminders. Pinned to the exact latest release (22.3.0) so this
  # project's version is distinct from all sibling apps in the workspace.
  flutter_local_notifications: 22.3.0
  flutter_timezone: ^5.1.0
  timezone: ^0.11.1
  firebase_core: any
  firebase_messaging: any
  flutter_secure_storage: any
  shared_preferences: any
  device_info_plus: any
  connectivity_plus: any
  webview_flutter_wkwebview: any
  url_launcher: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # Only processed runtime assets are bundled. The delivered source sheets under
  # assets/Featherpeak_Fury_APPLICATION_* stay out of the app.
  assets:
    - assets/images/
    - assets/sounds/
    - assets/legal/
    - assets/crestway/

  # Fonts are bundled instead of fetched at runtime so the app renders
  # correctly on first launch with no network connection.
  fonts:
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit-Regular.ttf
          weight: 400
        - asset: assets/fonts/Outfit-Medium.ttf
          weight: 500
        - asset: assets/fonts/Outfit-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Outfit-Bold.ttf
          weight: 700
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
          weight: 400
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700

flutter_launcher_icons:
  image_path: assets/icon/app_icon.png
  ios: true
  remove_alpha_ios: true
  android: ic_launcher
  adaptive_icon_background: assets/icon/adaptive_background.png
  adaptive_icon_foreground: assets/icon/adaptive_foreground.png
  min_sdk_android: 23
